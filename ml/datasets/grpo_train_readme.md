# GRPO train set

Generated by `python scripts/build_grpo_dataset.py` from `fixtures/*.json` only.

- Prompts: ≥12 (one per fixture)
- Upload to S3: `aws s3 cp ml/datasets/grpo_train.jsonl s3://$RAW_BUCKET/ml/grpo/train.jsonl`
- Bedrock RFT: attach `RewardFunction` ARN from SAM outputs, set **group size ≥ 13**

See [docs/ml.md](../docs/ml.md).
